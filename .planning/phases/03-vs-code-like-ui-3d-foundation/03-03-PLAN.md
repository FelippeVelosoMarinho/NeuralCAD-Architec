---
phase: 03-vs-code-like-ui-3d-foundation
plan: "03"
type: execute
wave: 3
depends_on:
  - "03-01"
  - "03-02"
files_modified:
  - services/web/package.json
  - services/web/src/components/viewport/MeshViewport.tsx
  - services/web/src/components/explorer/TopologyExplorer.tsx
  - services/web/src/hooks/useMeshFromJob.ts
  - services/worker/src/neuralcad_worker/tasks/geometry.py
  - services/api/src/neuralcad_api/main.py
  - README.md
autonomous: true
requirements:
  - UI-03
  - UI-04
user_setup:
  - Compose completo (API+worker+minio+db) para teste ponta-a-ponta opcional.
must_haves:
  truths:
    - "Viewport R3F exibe uma mesh STL carregada a partir da API."
    - "Explorer mostra hierarquia mínima alinhada a metadados de job/topologia sintética MVP."
  artifacts:
    - services/web/src/components/viewport/MeshViewport.tsx
    - services/worker geometria STL export paralelo
---

<objective>

**Viewport** com **React Three Fiber** + **`THREE.STLLoader`** (`three/examples`), carregamento assíncrono de STL servido pela API (**UI-03**). **Explorer** na sidebar lista nós sintéticos (Root → Corpo → Faces/Arestas/Vértices) derivados dos campos **`dimensional_audit`** + novo bloco opcional **`topologySketch`** (**UI-04**).

Extensões backend/worker obrigatórias no MVP técnico:

1. Worker gera **`model.stl`** (Ou mesh triangulada exportada OCC) paralelamente ao STEP e actualiza modelo job com segunda chave `artifact_stl_key` **OU** formato convencionado `{base}.stl` derivado deterministicamente como `artifact_key.replace('.step','.stl')` — escolha documentada nos commits mas **persistida em DB só se necessário** (se reuse path pattern, apenas convenção nome MinIO suficiente).
2. API **`GET /api/v1/jobs/{id}/artifacts/{kind}`** com `kind` em `{step,stl}` devolvendo `StreamingResponse`/bytes (`Content-Type` apropriado) autenticação MVP dev-only (**sem** auth forte — segue backlog).

Purpose: fecha sucesso roadmap Fase 3.

</objective>

<threat_model>
| threat_id | category | component | disposition | mitigation_plan |
|-----------|-----------|-----------|-------------|-----------------|
| T-UI-06 | Path traversal | filename | Mitigate | `kind` enum estrito — sem segmentos utilizador |
| T-UI-07 | SSRF artefato | presigned | Mitigate | Proxy só lê próprio bucket e prefixo jobs |
| T-UI-08 | Dos mesh | STL size | Mitigate | Limite tamanho (p.ex. <= 25MB soft) antes de THREE parse |
</threat_model>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@services/worker/src/neuralcad_worker/tasks/geometry.py
@services/worker/src/neuralcad_worker/storage/minio_client.py
@.planning/phases/03-vs-code-like-ui-3d-foundation/03-RESEARCH.md
@.planning/phases/03-vs-code-like-ui-3d-foundation/03-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Worker — export STL + topologySketch em dimensional_audit</name>
  <files>services/worker/src/neuralcad_worker/tasks/geometry.py</files>
  <read_first>services/worker/src/neuralcad_worker/geometry/stub_solid.py</read_first>
  <action>
Após TRIANGULATION:

- Tentar usar **STL export OCC** ou converter shape para STL bytes (Biblioteca occwl/pyocc exemplo mínimo) — se complexidade alta, teselar superfície com mesh grosso apenas para MVP.

Persistir segunda chave `jobs/{uuid}/model.stl` em MinIO (função já existente `put_object_bytes`).

Acrescentar a `dimensional_audit` objeto **`topologySketch`**: exemplo para caixa OCC:

```json
{"faces":6,"edges":12,"vertices":8}
```

Valores devem derivar sempre do shape OCC (`ShapeAnalysis_ShapeAnalyzer` rápido **ou contagem topo simplificada** documentada quando exactitude total arriscada — marcar campo `confidence:"mvp"` opcionalmente).

Alterar modelo SQLAlchemy `Job` apenas se segunda coluna obrigatória — preferir só `artifact_key` STEP + STL por convenção de nome fixo em mesma pasta (sem migrações se possível):

> convenção obrigatória: `stl_key = artifact_key[:-5] + '.stl'` quando `artifact_key.endswith('.step')`.

Se STL falhar, marcador **`dimensional_audit.mesh_error`** string não opaca.

Documentar comportamento README.

  </action>
  <verify>grep -q model.stl services/worker/src/neuralcad_worker/tasks/geometry.py</verify>
  <acceptance_criteria>
    - Após job sucesso, MinIO lista segundo objecto STL *ou* log explica falha mesh
  </acceptance_criteria>
  <done>Fonte binária para Three.</done>
</task>

<task type="auto">
  <name>Task 2: API — GET artefact streaming</name>
  <files>services/api/src/neuralcad_api/main.py, services/worker/pyproject.toml</files>
  <read_first>services/worker/src/neuralcad_worker/storage/minio_client.py</read_first>
  <action>

Adicionar rota **`GET /api/v1/jobs/{job_id}/artifacts/{kind}`** com Path `Literal['stl','step']`:

- Ler metadados job → `artifact_key` → resolver object key STL derivada se `kind=='stl'` senão usar key principal.

Reutilizar **boto3** cliente S3 já presente projeto worker — API precisa igual dependências (`boto3` já em API). Implementar cliente leitor simples (GET object) streams.

**CORS já plano anterior** permite fetch blob.

Responder `StreamingResponse`/bytes synchronous ok FastAPI primeiro.

  </action>
  <verify>grep -q artifacts services/api/src/neuralcad_api/main.py</verify>
  <acceptance_criteria>
    - `curl --fail` STL devoluído para job bem sucedido
  </acceptance_criteria>
  <done>Browser pode descarregar mesh MVP.</done>
</task>

<task type="auto">
  <name>Task 3: R3F MeshViewport + loader</name>
  <files>services/web/src/components/viewport/MeshViewport.tsx, services/web/src/hooks/useMeshFromJob.ts, services/web/package.json</files>
  <read_first>services/web/src/hooks/useJobFlow.ts</read_first>
  <action>

Instalar `three`, `@react-three/fiber`, `@react-three/drei`.

`MeshViewport.tsx`:

- `Canvas` R3F, luz ambient+directional (`drei`).
- Ao receber **`ArrayBuffer`/URL** STL — fetch via `same-origin-relative` proxy Vite usando base API + `/artifacts/stl`.
- Parsing `STLLoader` → `THREE.BufferGeometry` → `<mesh geometry={...}>` com material standard metálico leve (`meshPhysicalMaterial`).
- OrbitControls (drei) — limitações performance ok MVP.

Hook `useMeshFromJob`:

- Inputs: jobId quando status success.
- Efeitos: fetch STL arrayBuffer com `credentials: 'omit'`; guardar erro string.

Ao separador viewport ativo não forçar re-fetch infinitamente — usar cache simples keyed jobId.

  </action>
  <verify>grep -q STLLoader services/web/src/components/viewport/MeshViewport.tsx</verify>
  <acceptance_criteria>
    - Lint passes
  </acceptance_criteria>
  <done>UI-03 MVP.</done>
</task>

<task type="auto">
  <name>Task 4: TopologyExplorer ligado dimensional_audit</name>
  <files>services/web/src/components/explorer/TopologyExplorer.tsx, services/web/src/App.tsx</files>
  <read_first>services/web/src/hooks/useJobFlow.ts</read_first>
  <action>

Componente aceita objeto `DimensionalAuditRo` opcional espelho JSON.

Renderiza **react tree** expandable (somente primitives): root label `Jobs/{shortId}`, filhos sintéticos com contagens vindas `topologySketch` se presentes senão placeholders `--`.

Seleção elemento update store `selectedTopologyPath: string[]` em Zustand `shellStore` extensões.

**Seleção atual** destacada só visual MVP (não geometria highlighting — fase posterior).

Synchronização: sempre que novo job objeto polling actualiza dados.

  </action>
  <verify>grep -q TopologyExplorer services/web/src/components/explorer/TopologyExplorer.tsx</verify>
  <acceptance_criteria>
    - Estado global reflecte navegação clique sem erro console
  </acceptance_criteria>
  <done>UI-04 MVP.</done>
</task>

<task type="auto">
  <name>Task 5: Testes hooks + regressão rápida</name>
  <files>services/web/src/hooks/useMeshFromJob.test.ts</files>
  <read_first>services/web/vitest.config.ts</read_first>
  <action>
Test stub fetch devolvendo STL mínimo triangular bytes (fixture pequeno) validando geometria.vertices.count > 0 após loader (isolado com `THREE` headless opcional omitir — preferir smoke parse simples usando `three` apenas se rápido; senão apenas assert fetch ok blob size).

Rodar **`npm run test -- --run`**.

  </action>
  <verify>cd services/web &amp;&amp; npm run test -- --run</verify>
  <acceptance_criteria>
    - vitest OK
  </acceptance_criteria>
  <done>Rede regressão rápida.</done>
</task>

</tasks>

<verification>
- [ ] `docker compose run` smoke manual (opcional)
- [ ] vitest frontend
- [ ] pytest API se alterações model
</verification>

<success_criteria>

- Artefato gerado aparece viewport + árvore com contagens plausível caixa OCC.
</success_criteria>

<output>
`.planning/phases/03-vs-code-like-ui-3d-foundation/03-03-SUMMARY.md`
</output>

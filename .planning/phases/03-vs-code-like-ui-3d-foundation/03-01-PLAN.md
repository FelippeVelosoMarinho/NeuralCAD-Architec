---
phase: 03-vs-code-like-ui-3d-foundation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - services/web/package.json
  - services/web/vite.config.ts
  - services/web/tsconfig.json
  - services/web/tsconfig.node.json
  - services/web/index.html
  - services/web/src/main.tsx
  - services/web/src/App.tsx
  - services/web/src/styles/global.css
  - services/web/src/stores/shellStore.ts
  - services/web/src/components/layout/AppShell.tsx
  - services/web/vitest.config.ts
  - services/web/src/App.test.tsx
  - README.md
autonomous: true
requirements:
  - UI-01
user_setup:
  - Node 20+ e npm; `cd services/web && npm install` após checkout.
must_haves:
  truths:
    - "Existe `services/web` com Vite+React+TS e arranque (`npm run dev`) sem erros."
    - "Layout três regiões persistentes (sidebar, centro, inferior) com estado em Zustand."
  artifacts:
    - services/web/src/App.tsx
    - services/web/src/stores/shellStore.ts
  key_links:
    - "Árvore/layout visíveis segundo `03-UI-SPEC.md` (regiões nomeadas)."
---

<objective>
Bootstrap **`services/web`**: Vite 6 + React 18 + TypeScript, **Zustand** para estado global do *shell* (`sidebarOpen`, `bottomPanelOpen`, `activeCenterTab` placeholder), CSS **dark** com variáveis compatíveis com `03-UI-SPEC.md`, e **vitest + Testing Library** smoke test. Cumpre **UI-01** (percorrer layout sem corromper estado global verificável por teste de store + smoke render).

Purpose: Base para Monaco (plano 02) e viewport (plano 03).

Output: app executável `npm run dev` + `npm run test` verde mínimo.

Out of scope: Monaco, Three, chamadas API reais.

</objective>

<threat_model>
| threat_id | category | component | disposition | mitigation_plan |
|-----------|-----------|-----------|-------------|-----------------|
| T-UI-01 | Supply chain | npm deps | Mitigate | Pin major versions; `npm audit` antes de merge |
| T-UI-02 | XSS futuro | dangerouslySetInnerHTML | Mitigate | Proibido neste plano; editor virá com Monaco CSP-aware fase 2 |
</threat_model>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@IDEA.md
@.planning/phases/03-vs-code-like-ui-3d-foundation/03-CONTEXT.md
@.planning/phases/03-vs-code-like-ui-3d-foundation/03-RESEARCH.md
@.planning/phases/03-vs-code-like-ui-3d-foundation/03-UI-SPEC.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Vite React TS em services/web</name>
  <files>services/web/package.json, services/web/vite.config.ts, services/web/tsconfig.json, services/web/tsconfig.node.json, services/web/index.html, services/web/src/main.tsx</files>
  <read_first>.planning/phases/03-vs-code-like-ui-3d-foundation/03-RESEARCH.md</read_first>
  <action>
Criar projeto Vite **React + TypeScript** em `services/web/` (manual ou `npm create vite@latest` seguido mover). Garantir scripts: `dev`, `build`, `preview`, `test`, `lint` (eslint pode ser mínimo `--max-warnings 0` placeholder).

Configurar **`VITE_API_BASE_URL`** em `import.meta.env` com default `http://127.0.0.1:8000` em `.env.development` (ficheiro exemplo comentado README — não commit secrets).

`strict: true` tsconfig.
  </action>
  <verify>test -f services/web/package.json && test -f services/web/vite.config.ts</verify>
  <acceptance_criteria>
    - `cd services/web && npm install && npm run build` conclui sem erro
  </acceptance_criteria>
  <done>Scaffold compilável.</done>
</task>

<task type="auto">
  <name>Task 2: Zustand shellStore + AppShell layout</name>
  <files>services/web/src/stores/shellStore.ts, services/web/src/components/layout/AppShell.tsx, services/web/src/App.tsx, services/web/src/styles/global.css</files>
  <read_first>@.planning/phases/03-vs-code-like-ui-3d-foundation/03-UI-SPEC.md</read_first>
  <action>
Implementar **Zustand store** `shellStore` com ações: toggle sidebar, toggle painel inferior, `setCenterTab` (**valores** `'prompt' | 'viewport'` — viewport tab pode mostrar placeholder texto “Viewport (fase seguinte)”).

`AppShell.tsx` compõe CSS grid/flex:

- **Sidebar** esquerda: título “Explorer” + texto placeholder “Topo (fase seguinte)”.
- **Centro:** zona de tabs simulada (botões) Prompt | 3D conforme store.
- **Inferior:** zona “Copilot / Job” com estado placeholder.

`global.css` define variáveis CSS dark alinhadas semanticamente a `03-UI-SPEC.md`.

Garantir toggles **não reinicializam estado** ao clicar várias vezes (pure functions no store).
  </action>
  <verify>grep -q shellStore services/web/src/stores/shellStore.ts</verify>
  <acceptance_criteria>
    - Sidebar e painel inferior podem colapsar/expander sem remount completo da árvore React (inspeção código)
  </acceptance_criteria>
  <done>UI-01 comportamento base implementado.</done>
</task>

<task type="auto">
  <name>Task 3: Vitest + smoke test</name>
  <files>services/web/vitest.config.ts, services/web/src/App.test.tsx</files>
  <read_first>services/web/package.json</read_first>
  <action>
Adicionar **vitest**, **@testing-library/react**, **jsdom**. Configurar `happy-dom` ou `jsdom` environment.

Test `App.test.tsx`: render `App` → asserir presença texto “Explorer” ou região definida + que botão toggle sidebar altera `aria-expanded` ou class condicional (definir marcador de teste `data-testid`).

Comando: `cd services/web && npm run test -- --run`.
  </action>
  <verify>cd services/web &amp;&amp; npm run test -- --run</verify>
  <acceptance_criteria>
    - Saída pytest/vitest exit code 0
  </acceptance_criteria>
  <done>Wave 0 da validação arrancada.</done>
</task>

<task type="auto">
  <name>Task 4: README raiz — secção Frontend</name>
  <files>README.md</files>
  <read_first>README.md</read_first>
  <action>
Documentar 4–6 linhas: `cd services/web && npm install && npm run dev` (porta 5173 habitual), variável `VITE_API_BASE_URL`, ligação ao backend fase 2.
  </action>
  <verify>grep -q "services/web" README.md</verify>
  <acceptance_criteria>
    - README menciona `services/web` e comando `npm run dev`
  </acceptance_criteria>
  <done>Orientação onboarding.</done>
</task>

</tasks>

<verification>
- [ ] `cd services/web && npm run test -- --run`
- [ ] `cd services/web && npm run build`
</verification>

<success_criteria>
- UI-01 verificável por teste + navegação toggles
- Base pronta para planos 03-02 e 03-03 sem refatoração destrutiva
</success_criteria>

<output>
Após conclusão criar `.planning/phases/03-vs-code-like-ui-3d-foundation/03-01-SUMMARY.md`
</output>

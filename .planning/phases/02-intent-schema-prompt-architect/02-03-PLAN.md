---
phase: 02-intent-schema-prompt-architect
plan: "03"
type: execute
wave: 3
depends_on:
  - "02-01"
  - "02-02"
files_modified:
  - services/api/src/neuralcad_api/main.py
  - services/api/src/neuralcad_api/schemas/jobs.py
  - services/api/src/neuralcad_api/services/payload_normalize.py
  - services/api/src/neuralcad_api/routers/__init__.py
  - services/api/src/neuralcad_api/routers/intent.py
  - services/api/tests/test_jobs_intent_flow.py
  - services/worker/src/neuralcad_worker/tasks/geometry.py
  - README.md
  - docker-compose.yml
  - .env.example
autonomous: true
requirements:
  - INTENT-01
  - INTENT-02
  - INTENT-03
  - INTENT-04
user_setup:
  - "Copiar `.env.example` → `.env` e definir `ANTHROPIC_API_KEY` para testes manuais de `POST /api/v1/intent/elicit`."
must_haves:
  truths:
    - "`POST /api/v1/jobs` só aceita corpo compatível com `IntentJobEnvelope`/`IntentSchemaV1` + `preflight` (sem `extra=allow`)."
    - "Worker resolve `constraints.dimensionsMm` antes do fallback `intent.constraints`."
  artifacts:
    - services/api/src/neuralcad_api/main.py
    - services/worker/src/neuralcad_worker/tasks/geometry.py
  key_links:
    - "README menciona fluxo `elicit` → `jobs` ou campos necessários."
---

<objective>
Ligar HTTP: **`POST /api/v1/intent/elicit`** (NL → serviço plano 02), **`POST /api/v1/jobs`** com payload **Intent validado**, normalização opcional **legado**, persistência **`preflight.geo_risk`**, e actualizar **worker** para shape canónico (**INTENT-03** ingestão generationConfig + dims). Satisfaz **INTENT-01–04** ponta-a-ponta ao nível API+worker+MVP docs.

Purpose: Fechar roadmap Fase 2 plans 02-01..03.

Output: routers, schemas job novos, testes integração leves, compose env pass-through opcional.
</objective>

<threat_model>
| threat_id | category | component | disposition | mitigation_plan |
|-----------|-----------|-----------|-------------|-------------------|
| T-PA-06 | Injection | NL prompt | Mitigate | Não executar conteúdo NL; só passar a API Anthropic |
| T-PA-07 | Information disclosure | Erros 422 | Mitigate | Mensagens sem stack trace interno em produção |
| T-PA-08 | Spoofing | Jobs sem auth | Mitigate | Manter TODO auth fase futura; documentar dev-only |
</threat_model>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-intent-schema-prompt-architect/02-CONTEXT.md
@services/api/src/neuralcad_api/main.py
@services/api/src/neuralcad_api/schemas/jobs.py
@services/api/src/neuralcad_api/services/prompt_architect.py
@services/api/src/neuralcad_api/schemas/intent_v1.py
@services/worker/src/neuralcad_worker/tasks/geometry.py
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Normalização + IntentJobEnvelope + reforma jobs.py</name>
  <files>services/api/src/neuralcad_api/services/payload_normalize.py, services/api/src/neuralcad_api/schemas/jobs.py</files>
  <read_first>services/api/src/neuralcad_api/schemas/jobs.py, services/api/src/neuralcad_api/schemas/intent_v1.py, .planning/phases/02-intent-schema-prompt-architect/02-CONTEXT.md</read_first>
  <action>
Criar `payload_normalize.py` com função **`normalize_legacy_intent_payload(data: dict) -> dict`**:
- Se existir `intent.constraints.dimensionsMm` e **não** existir `constraints.dimensionsMm`, copiar bloco para `constraints.dimensionsMm` no nível raiz.
- Definir `schemaVersion: "1"` no output se ausente.
- **Não** remover chaves desconhecidas antes do primeiro parse — quem valida é Pydantic com `forbid` após normalização mínima.

Substituir `JobCreate` por **`IntentJobEnvelope`** (nome pode ser `JobCreateIntent`): campos `intent_schema: IntentSchemaV1` (Field `alias="intent"` **ou** nome `intent` com tipo `IntentSchemaV1` conforme OpenAPI legível) e **`preflight: dict | None`** onde `preflight` **deve** conter chave `geo_risk` serializável (tipo `dict` validado manualmente ou modelo `GeoRisk` reutilizado de `elicitation.py`) quando fornecido pelo cliente após elicit.

`JobResponse` permanece; garantir que `payload_json` exposto reflecte o armazenado.

Remover `extra="allow"` do modelo de criação de job de produção.
  </action>
  <verify>grep -q IntentSchemaV1 services/api/src/neuralcad_api/schemas/jobs.py || grep -q intent_v1 services/api/src/neuralcad_api/schemas/jobs.py</verify>
  <acceptance_criteria>
    - `schemas/jobs.py` importa ou referencia `IntentSchemaV1`
    - `schemas/jobs.py` **não** contém `extra="allow"` no modelo de request de criação de job (removido ou substituído)
    - `payload_normalize.py` contém string literal `dimensionsMm`
  </acceptance_criteria>
  <done>Contrato HTTP alinhado INTENT-01.</done>
</task>

<task type="auto">
  <name>Task 2: Router intent + integração main + persistência preflight</name>
  <files>services/api/src/neuralcad_api/routers/__init__.py, services/api/src/neuralcad_api/routers/intent.py, services/api/src/neuralcad_api/main.py, docker-compose.yml, .env.example</files>
  <read_first>services/api/src/neuralcad_api/main.py, services/api/src/neuralcad_api/services/prompt_architect.py, services/api/src/neuralcad_api/schemas/elicitation.py</read_first>
  <action>
Criar `routers/intent.py` com **`POST /api/v1/intent/elicit`**:
- Request body Pydantic: `{"prompt": str, "attempt": int = 1}` (nomes exatos ajustáveis; documentar).
- Instanciar `PromptArchitectService` (ou dependência FastAPI singleton simples).
- Retornar **200** com JSON `ElicitSuccess.model_dump()` quando sucesso.
- Retornar **422** com corpo `ElicitClarification.model_dump()` quando clarificação (não usar 500).
- Retornar **422** com `ElicitRejected` quando rejeitado.

Em `main.py`: `app.include_router(..., prefix="" )` ou prefixo vazio de forma que path final seja `/api/v1/intent/elicit`.

Alterar **`create_job`** para:
- Aceitar `IntentJobEnvelope`.
- Opcional: chamar `normalize_legacy_intent_payload` só se **query** `?legacy_normalize=1` ou cabeçalho opt-in — **recomendado:** aplicar normalização **sempre** antes de validar **apenas** quando `Content-Type` JSON falha primeira validação (retry path) OU documentar cliente deve pré-normalizar — **implementação simples obrigatória:** tentar parse directo `IntentJobEnvelope`; em `ValidationError` única vez, aplicar `normalize_legacy_intent_payload` e retry (máximo 1 retry) conforme **02-CONTEXT D-03**.

Ao persistir job: serializar **`IntentSchemaV1.model_dump(by_alias=True)`** dentro de `payload_json`; acrescentar `preflight` com `geo_risk` copiado do envelope.

Compose: sob `services.api.environment` acrescentar `ANTHROPIC_API_KEY`, `ANTHROPIC_MODEL` opcionais (documentar placeholders vazios desactivam elicit runtime errors — preferir opcional compose comment).

Actualizar README secção API com exemplos curl mínimos para `elicit` e `jobs`.
  </action>
  <verify>grep -r "intent/elicit" services/api/src/</verify>
  <acceptance_criteria>
    - `main.py` ou `routers/intent.py` contém literal `/api/v1/intent/elicit`
    - `main.py` contém literal `preflight` ou gravação equivalente em comentário+código visível em grep
    - `README.md` contém substring `intent` e `elicit` (case insensitive basta uma linha com ambos — usar frase "POST /api/v1/intent/elicit")
  </acceptance_criteria>
  <done>INTENT-02 end-to-end HTTP + INTENT-04 snapshot.</done>
</task>

<task type="auto">
  <name>Task 3: Worker dims canónicas + testes integração jobs</name>
  <files>services/worker/src/neuralcad_worker/tasks/geometry.py, services/api/tests/test_jobs_intent_flow.py</files>
  <read_first>services/worker/src/neuralcad_worker/tasks/geometry.py, services/api/src/neuralcad_api/schemas/intent_v1.py, services/api/src/neuralcad_api/main.py</read_first>
  <action>
Refactor **`_parse_target_dims`**:
- Primeiro ler `payload.get("constraints")` ou `payload.get("constraints",{})` com `dimensionsMm`.
- Fallback: path legado `intent → constraints → dimensionsMm`.
- Manter target dict no audit alinhado ao formato actual (`target.dimensionsMm`).

Criar `test_jobs_intent_flow.py` com **TestClient** FastAPI:
- Override dependência DB **ou** usar SQLite in-memory se project já suportar — **mínimo viável:** mock `async_session_maker`/session que grava job em lista em memória e assert `payload_json["preflight"]["geo_risk"]` quando fornecido. Se mocking async DB for pesado: testar apenas **schemas** conversão envelope→dict+e **normalize_legacy** neste ficheiro, e uma app `FastAPI()` mínima com só router intent mockando serviço.

**Critério mínimo obrigatório:** pelo menos um teste que chama **`normalize_legacy_intent_payload`** demonstrando cópia de nested→root e parse `IntentJobEnvelope` bem-sucedido.

Executar `cd services/api && python -m pytest -q --tb=short tests/`.
  </action>
  <verify>cd services/api &amp;&amp; python -m pytest -q --tb=short tests/</verify>
  <acceptance_criteria>
    - `geometry.py` contém comentário ou código que menciona literal `constraints` antes de `intent` na docstring OU duas ramificações de leitura visíveis
    - Full `pytest tests/` desde `services/api/` exit code 0
  </acceptance_criteria>
  <done>INTENT-03 rastreabilidade worker + regressão testes.</done>
</task>

</tasks>

<verification>
- [ ] `pytest` completo em `services/api/tests/` passa
- [ ] `docker compose config` continua válido na raiz (se compose alterado)
</verification>

<success_criteria>
- Quatro requisitos INTENT-01..04 verificáveis por testes e inspecção
- Worker compatível com payload canónico pós-Fase 2
</success_criteria>

<output>
Após conclusão criar `.planning/phases/02-intent-schema-prompt-architect/02-03-SUMMARY.md`
</output>

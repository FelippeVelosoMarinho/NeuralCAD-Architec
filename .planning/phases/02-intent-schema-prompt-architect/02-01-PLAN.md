---
phase: 02-intent-schema-prompt-architect
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - services/api/pyproject.toml
  - services/api/src/neuralcad_api/schemas/intent_v1.py
  - services/api/tests/conftest.py
  - services/api/tests/test_intent_schema.py
autonomous: true
requirements:
  - INTENT-01
user_setup: []
must_haves:
  truths:
    - "Modelo raiz `IntentSchemaV1` rejeita chaves desconhecidas (`extra=\"forbid\"` no equivalente Pydantic v2)."
    - "`pytest` nos testes de contrato passa localmente."
  artifacts:
    - services/api/src/neuralcad_api/schemas/intent_v1.py
    - services/api/tests/test_intent_schema.py
  key_links:
    - "Estrutura de campos espelha `IDEA.md` § IntentSchemaV1 (irmãos ao nível raiz: `intent`, `constraints`, ...)."
---

<objective>
Modelos **Pydantic v2** equivalentes a **`IntentSchemaV1`** com `extra="forbid"` no tipo raiz público, cobrindo **INTENT-01**. Inclui testes automatizados que provam aceitação canónica e rejeição de campos extra.

Purpose: Base estática para Prompt Architect e POST `/jobs`; sem chamadas LLM neste plano.

Output: módulo `intent_v1.py` + `pytest` + deps de dev.
</objective>

<threat_model>
**Trust boundaries:** Apenas validação local; sem dados sensíveis nos fixtures.

| threat_id | category | component | disposition | mitigation_plan |
|-----------|-----------|-----------|-------------|-------------------|
| T-PA-01 | Elevation | Parsing leniente | Mitigate | `forbid` extra; testes negativos obrigatórios |
| T-PA-02 | Tampering | Fixture JSON | Mitigate | Não incluir API keys em ficheiros de teste |
</threat_model>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@IDEA.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-intent-schema-prompt-architect/02-CONTEXT.md
@.planning/phases/02-intent-schema-prompt-architect/02-RESEARCH.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: pyproject dev + schemas IntentSchemaV1</name>
  <files>services/api/pyproject.toml, services/api/src/neuralcad_api/schemas/intent_v1.py</files>
  <read_first>IDEA.md, .planning/phases/02-intent-schema-prompt-architect/02-CONTEXT.md, services/api/pyproject.toml</read_first>
  <action>
Adicionar a `services/api/pyproject.toml` em `[project.optional-dependencies]` secção **`dev`** (criar ou fundir): `pytest`, `httpx`.

Criar `services/api/src/neuralcad_api/schemas/intent_v1.py` exportando pelo menos:

- **`DimensionsMm`** com `width`, `height`, `depth` tipo `float` (todos obrigatórios quando o bloco existe; objecto opcional no pai).
- **`IntentBlock`**: `object_type: str` (Field alias `objectType` com `serialization_alias`/`validation_alias` conforme padrão Pydantic v2 JSON), `style: list[str]` default fábrica `list`, `functional_goal: str` (alias `functionalGoal`).
- **`ConstraintsBlock`**: `dimensions_mm: Optional[DimensionsMm]` (alias `dimensionsMm`), `thickness_mm: Optional[float]` (`thicknessMm`), `symmetry: Optional[str]`, `manufacturing_hints: list[str]` (`manufacturingHints`), `material_hints: list[str]` (`materialHints`).
- **`HoleHint`**, **`TopologyHints`**, **`SurfaceHints`**, **`GenerationConfig`**, **`QualityTargets`** com campos espelhando **nomes e tipos** do JSON em `IDEA.md` (usar `Optional` e defaults seguros onde o exemplar JSON usa `0` apenas como placeholder — documentar no docstring que dimensões `0` são inválidas para job real e serão validadas no plano 03).
- **`IntentSchemaV1`**: `model_config = ConfigDict(extra="forbid", populate_by_name=True)`; campos raiz `session_id` (`sessionId`), `prompt_original` (`promptOriginal`), `intent: IntentBlock`, `constraints: ConstraintsBlock`, `topology_hints` (`topologyHints`) opcional, `surface_hints` (`surfaceHints`) opcional, `generation_config` (`generationConfig`) opcional, `quality_targets` (`qualityTargets`) opcional.

Incluir **`model_validate_json`** usável nos testes. Não integrar ainda `main.py`.
  </action>
  <verify>python -c "from neuralcad_api.schemas.intent_v1 import IntentSchemaV1" 2>/dev/null || true</verify>
  <acceptance_criteria>
    - O ficheiro `services/api/src/neuralcad_api/schemas/intent_v1.py` contém a string literal `class IntentSchemaV1`
    - O mesmo ficheiro contém a string literal `extra="forbid"` ou `extra='forbid'` dentro de `ConfigDict`
    - `services/api/pyproject.toml` contém a substring `pytest`
  </acceptance_criteria>
  <done>Módulo de schema importável e dependências de teste declaradas.</done>
</task>

<task type="auto">
  <name>Task 2: Testes de contrato INTENT-01</name>
  <files>services/api/tests/conftest.py, services/api/tests/test_intent_schema.py</files>
  <read_first>services/api/src/neuralcad_api/schemas/intent_v1.py</read_first>
  <action>
Criar `services/api/tests/conftest.py` mínimo (pode estar vazio exceto comentário de pacote).

Criar `services/api/tests/test_intent_schema.py` com:
1. **test_accepts_minimal_canonical_shape** — JSON string com **todas** as chaves raiz presentes em `IDEA.md` (valores numéricos &gt; 0 em `dimensionsMm` para ser semanticamente válido), `IntentSchemaV1.model_validate_json` sem erro.
2. **test_rejects_unknown_top_level_key** — mesmo JSON com chave extra `"evil": 1` deve levantar `ValidationError`.
3. **test_roundtrip_serialization** — instância válida → `model_dump(by_alias=True)` contém chave literal `"sessionId"` e `"constraints"`.

Executar `cd services/api && pip install -e ".[dev]" && python -m pytest -q --tb=short tests/test_intent_schema.py`.
  </action>
  <verify>cd services/api &amp;&amp; python -m pytest -q --tb=short tests/test_intent_schema.py</verify>
  <acceptance_criteria>
    - Comando `python -m pytest -q tests/test_intent_schema.py` desde `services/api/` termina com **exit code 0**
    - `test_intent_schema.py` contém a string literal `unknown` ou `extra` ou `ValidationError`
  </acceptance_criteria>
  <done>INTENT-01 coberto por testes automatizados.</done>
</task>

</tasks>

<verification>
Antes de declarar o plano completo:
- [ ] `pytest tests/test_intent_schema.py` passa dentro de `services/api/`
- [ ] Nenhuma dependência anthropic neste plano (só Pydantic)
</verification>

<success_criteria>
- INTENT-01 satisfied: validação estrita e exemplos IDEA cobertos nos testes
- Nenhuma alteração a `main.py` ou worker neste plano
</success_criteria>

<output>
Após conclusão criar `.planning/phases/02-intent-schema-prompt-architect/02-01-SUMMARY.md`
</output>

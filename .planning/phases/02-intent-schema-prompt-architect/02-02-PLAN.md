---
phase: 02-intent-schema-prompt-architect
plan: "02"
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - services/api/pyproject.toml
  - services/api/src/neuralcad_api/schemas/elicitation.py
  - services/api/src/neuralcad_api/services/__init__.py
  - services/api/src/neuralcad_api/services/prompt_architect.py
  - services/api/tests/test_prompt_architect.py
  - .env.example
autonomous: true
requirements:
  - INTENT-02
  - INTENT-04
user_setup:
  - "Definir `ANTHROPIC_API_KEY` no `.env` local para testes manuais com API real (opcional)."
must_haves:
  truths:
    - "Serviço invoca cliente Anthropic via env `ANTHROPIC_API_KEY` e não em código."
    - "Respostas de clarificação / rejeição são Pydantic-verificáveis."
  artifacts:
    - services/api/src/neuralcad_api/services/prompt_architect.py
    - services/api/src/neuralcad_api/schemas/elicitation.py
  key_links:
    - "Modelo de sucesso valida contra `IntentSchemaV1` importado de `intent_v1.py`."
---

<objective>
Implementar **Prompt Architect**: serviço que chama Claude, exige **JSON estruturado**, produz **`IntentSchemaV1` validado** ou **`clarification`** ou **`rejected`**, e anexa **`geo_risk`** coerente com INTENT-04. **INTENT-02** e **INTENT-04** ao nível serviço (sem rotas HTTP — plano 03).

Purpose: Isolar LLM, timeouts e parsing para testes com mock.

Output: `prompt_architect.py`, modelos de elicitação, testes com `unittest.mock`, dependência `anthropic`, variáveis `.env.example`.
</objective>

<threat_model>
| threat_id | category | component | disposition | mitigation_plan |
|-----------|-----------|-----------|-------------|-------------------|
| T-PA-03 | Information disclosure | Prompts em logs | Mitigate | Não logar corpo completo por default; nivel DEBUG opcional |
| T-PA-04 | Tampering | Resposta modelo | Mitigate | Validar com Pydantic; regex JSON strip opcional |
| T-PA-05 | Elevation | API key | Mitigate | Ler só de env; nunca commitar |
</threat_model>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@IDEA.md
@.planning/phases/02-intent-schema-prompt-architect/02-CONTEXT.md
@.planning/phases/02-intent-schema-prompt-architect/02-RESEARCH.md
@services/api/src/neuralcad_api/schemas/intent_v1.py
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dependência anthropic + modelos elicitation + GeoRisk</name>
  <files>services/api/pyproject.toml, services/api/src/neuralcad_api/schemas/elicitation.py</files>
  <read_first>services/api/pyproject.toml, .planning/phases/02-intent-schema-prompt-architect/02-CONTEXT.md, services/api/src/neuralcad_api/schemas/intent_v1.py</read_first>
  <action>
Adicionar `anthropic>=0.40` a `dependencies` em `services/api/pyproject.toml`.

Criar `services/api/src/neuralcad_api/schemas/elicitation.py` com:
- **`GeoRisk`**: `severity: Literal["info","warn","critical"]`, `messages: list[str]` (mínimo 1 quando instanciado), opcional `related_field: str | None`.
- **`ElicitSuccess`**: `kind: Literal["success"]`, `intent: IntentSchemaV1`, `geo_risk: GeoRisk`, `attempt: int`.
- **`ElicitClarification`**: `kind: Literal["clarification_needed"]`, `questions: list[str]` (máx 3 itens validados com field validator ou constração antes de construir), `missing_fields: list[str]` default `[]`, `attempt: int`, `max_attempts: int = 2`.
- **`ElicitRejected`**: `kind: Literal["rejected"]`, `reason: str`.

Tipagem de união **`ElicitResult`** (discriminatório por `kind`).

Actualizar `.env.example` com linhas sem segredos: `ANTHROPIC_API_KEY=`, `ANTHROPIC_MODEL=claude-sonnet-4-20250514` (ajustar string ao modelo desejado; manter formato env).
  </action>
  <verify>grep -q anthropic services/api/pyproject.toml</verify>
  <acceptance_criteria>
    - `services/api/pyproject.toml` contém a substring literal `anthropic`
    - `elicitation.py` contém `GeoRisk`
    - `elicitation.py` contém literal `clarification_needed`
    - `.env.example` contém linha iniciando por `ANTHROPIC_API_KEY=`
  </acceptance_criteria>
  <done>Contratos de resposta locked.</done>
</task>

<task type="auto">
  <name>Task 2: PromptArchitectService + testes mock</name>
  <files>services/api/src/neuralcad_api/services/__init__.py, services/api/src/neuralcad_api/services/prompt_architect.py, services/api/tests/test_prompt_architect.py</files>
  <read_first>services/api/src/neuralcad_api/schemas/elicitation.py, services/api/src/neuralcad_api/schemas/intent_v1.py</read_first>
  <action>
Criar pacote `services` com `__init__.py` vazio ou export do serviço.

Implementar **`PromptArchitectService`** com método **`elicit(prompt: str, attempt: int = 1) -> ElicitResult`**:
- Lê `ANTHROPIC_API_KEY`, `ANTHROPIC_MODEL` de `os.environ`; se key ausente, levantar **`RuntimeError`** claro (testável).
- System prompt (constante módulo) exige: saída **apenas** JSON com discriminação `kind` ∈ {`success`, `clarification_needed`, `rejected`} e campos alinhados aos modelos; proibição de markdown fora do JSON.
- Em `kind=success`, campo `intent` deve ser objecto que passa `IntentSchemaV1.model_validate`.
- Quando `attempt &gt;= max_attempts` (2) e modelo devolveria clarificação, normalizar para **`ElicitRejected`** com razão explícita (lógica em Python após parse).
- Anexar **`geo_risk`** em sucessos: se `quality_targets` mencionar risco alto, `severity=warn`, senão `info` (regra simples documentada em comentário).

Testes `test_prompt_architect.py`:
- **test_missing_api_key** — RuntimeError.
- **test_mock_success_path** — patch `Anthropic.messages.create` devolvendo JSON `success` mínimo válido; assert `ElicitSuccess`.
- **test_mock_clarification** — retorno `clarification_needed` com ≤3 perguntas.

Correr `cd services/api && python -m pytest -q --tb=short tests/test_prompt_architect.py`.
  </action>
  <verify>cd services/api &amp;&amp; python -m pytest -q --tb=short tests/test_prompt_architect.py</verify>
  <acceptance_criteria>
    - `prompt_architect.py` contém `Anthropic`
    - `prompt_architect.py` contém `elicit`
    - `pytest` tests/test_prompt_architect.py exit code 0
  </acceptance_criteria>
  <done>Serviço LLM testável sem rede.</done>
</task>

</tasks>

<verification>
- [ ] `pytest tests/test_prompt_architect.py` passa
- [ ] Nenhuma rota FastAPI adicionada neste plano
</verification>

<success_criteria>
- INTENT-02: NL→estrutura ou clarificação via serviço
- INTENT-04: `geo_risk` presente no ramo de sucesso
</success_criteria>

<output>
Após conclusão criar `.planning/phases/02-intent-schema-prompt-architect/02-02-SUMMARY.md`
</output>

---
phase: 01-geometric-pipeline-platform
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - docker-compose.yml
  - .env.example
  - README.md
  - services/api/Dockerfile
  - services/worker/Dockerfile
autonomous: true
requirements:
  - FOUND-01
user_setup: []
must_haves:
  truths:
    - "`docker compose config` valida sem erros a partir da raiz do repositório."
    - "Os serviços `postgres`, `redis`, `minio`, `api`, `worker` estão definidos no Compose com healthcheck ou `depends_on` coerente."
  artifacts:
    - docker-compose.yml
    - .env.example
    - README.md
  key_links:
    - "README descreve o comando `docker compose up --build` e portas expostas."
---

<objective>
Arranque local **reproduzível** (FOUND-01): `docker-compose.yml` na raiz com PostgreSQL 16, Redis, MinIO, imagens **`api`** (FastAPI) e **`worker`** (Celery) com Dockerfiles mínimos (Python 3.10), variáveis via `.env.example` (sem segredos reais), e README com instruções de subida e portas padrão (`api: 8000`, `postgres: 5432`, `redis: 6379`, `minio: 9000` API S3, `9001` console opcional).

Purpose: Base para todas as tarefas seguintes; falha aqui bloqueia a fase.

Output: Ficheiros de infra listados em `files_modified` com conteúdo concreto.
</objective>

<threat_model>
**Trust boundaries:** Devidores nas máquinas de desenvolvimento; rede Docker interna; MinIO/Postgres não expostos à Internet em produção (fora de escopo, mas não documentar credenciais default fortes como “produção”).

| threat_id | category | component | disposition | mitigation_plan |
|-----------|-----------|-----------|-------------|-----------------|
| T-01 | Tampering | MinIO/Postgres volumes | Mitigate | Credenciais apenas via env; `.env` no `.gitignore`; `.env.example` sem passwords reais |
| T-02 | Information disclosure | Logs Compose | Mitigate | README: não commitar `.env`; não logar secrets |
| T-03 | Elevation / misconfig | Port exposure | Mitigate | Bind 127.0.0.1 onde possível para DB/Redis em dev (documentar) |
</threat_model>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-geometric-pipeline-platform/01-CONTEXT.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Docker Compose e variáveis de ambiente</name>
  <files>docker-compose.yml, .env.example, README.md</files>
  <read_first>.planning/PROJECT.md, .planning/phases/01-geometric-pipeline-platform/01-CONTEXT.md</read_first>
  <action>Criar na raiz `docker-compose.yml` com serviços: `postgres` (imagem `postgres:16`), `redis` (imagem `redis:7-alpine`), `minio` (imagem `minio/minio:latest` com comando `server /data --console-address ":9001"`), build `api` de `./services/api`, build `worker` de `./services/worker`. Definir redes e volumes nomeados para Postgres, Redis e MinIO. Usar variáveis `${POSTGRES_USER}`, `${POSTGRES_PASSWORD}`, `${POSTGRES_DB}`, `${MINIO_ROOT_USER}`, `${MINIO_ROOT_PASSWORD}` lidas do env. Criar `.env.example` com valores placeholder **literais**: `POSTGRES_USER=neuralcad`, `POSTGRES_PASSWORD=change_me`, `POSTGRES_DB=neuralcad`, `MINIO_ROOT_USER=minio`, `MINIO_ROOT_PASSWORD=change_me_minio`, `REDIS_URL=redis://redis:6379/0`, `DATABASE_URL=postgresql+asyncpg://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}` (documentar que em ficheiro real se substitui). Adicionar `README.md` com secção `## Local (Docker)` contendo exactamente as strings de comando: `docker compose up --build` e `docker compose config`. Mencionar portas: API **8000**, Postgres **5432**, Redis **6379**, MinIO **9000** (S3) e **9001** (consola opcional).</action>
  <verify>grep -E 'postgres:16|redis:7|minio/minio' docker-compose.yml; grep -q 'docker compose up --build' README.md</verify>
  <acceptance_criteria>
    - `docker-compose.yml` contém a string literal `postgres:16`
    - `docker-compose.yml` contém a string literal `services/worker` ou context `./services/worker` (build worker)
    - `.env.example` contém a linha exata `POSTGRES_DB=neuralcad`
    - `README.md` contém a substring `docker compose up --build`
  </acceptance_criteria>
  <done>Compose + env example + README verificáveis pelos critérios acima.</done>
</task>

<task type="auto">
  <name>Task 2: Dockerfiles placeholder API e Worker</name>
  <files>services/api/Dockerfile, services/worker/Dockerfile</files>
  <read_first>docker-compose.yml</read_first>
  <action>Criar `services/api/Dockerfile` base `python:3.10-slim` com `WORKDIR /app`, `COPY` preparado para código futuro, `EXPOSE 8000`, e comando placeholder `CMD ["python", "-c", "print('api placeholder')"]` **ou** `CMD ["sleep", "infinity"]` apenas se o README explicar que serão substituídos no plano 01-02 — preferir `python -m http.server` NÃO usar; usar `CMD ["uvicorn", ...]` só após ter `pyproject` no plano seguinte. Para este plano, usar `CMD ["sh", "-c", "echo api stub && sleep infinity"]` para o contentor permanecer vivo e `docker compose up` não sair com erro imediato, **e** documentar no README que é stub até 01-02. Criar `services/worker/Dockerfile` análogo com `CMD ["sh", "-c", "echo worker stub && sleep infinity"]`.</action>
  <verify>test -f services/api/Dockerfile &amp;&amp; test -f services/worker/Dockerfile</verify>
  <acceptance_criteria>
    - `services/api/Dockerfile` contém `FROM python:3.10`
    - `services/worker/Dockerfile` contém `FROM python:3.10`
    - Ambos contêm `sleep infinity` ou mecanismo equivalente documentado no README como stub
  </acceptance_criteria>
  <done>Dois Dockerfiles presentes e alinhados com Compose.</done>
</task>

</tasks>

<verification>
Antes de declarar o plano completo:
- [ ] `docker compose config` executado na raiz termina com exit code 0
- [ ] `grep` confirma serviços postgres, redis, minio, api, worker em `docker-compose.yml`
</verification>

<success_criteria>

- Todas as tarefas concluídas
- FOUND-01 satisfeito ao nível de infra documentada e válida
- Nenhum segredo real committed
  </success_criteria>

<output>
Após conclusão, criar `.planning/phases/01-geometric-pipeline-platform/01-01-SUMMARY.md`
</output>

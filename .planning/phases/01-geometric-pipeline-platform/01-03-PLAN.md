---
phase: 01-geometric-pipeline-platform
plan: "03"
type: execute
wave: 3
depends_on:
  - "01-02"
files_modified:
  - services/worker/pyproject.toml
  - services/worker/Dockerfile
  - services/worker/src/neuralcad_worker/celery_app.py
  - services/worker/src/neuralcad_worker/tasks/geometry.py
  - services/worker/src/neuralcad_worker/geometry/stub_solid.py
  - services/worker/src/neuralcad_worker/geometry/measure.py
  - services/worker/src/neuralcad_worker/storage/minio_client.py
  - services/worker/src/neuralcad_worker/db/sync_session.py
  - docker-compose.yml
autonomous: true
requirements:
  - FOUND-02
  - FOUND-04
user_setup: []
must_haves:
  truths:
    - "Worker recebe task com UUID, gera sólido (stub OCC), exporta STEP para MinIO, calcula `dimensional_audit` com `source: \"pythonocc\"`, atualiza Postgres `success` + `artifact_key` + JSONB."
    - "Tolerância D-04 aplicada em código: `withinTolerance` só quando target numérico presente; desvios não mudam status para `failed`."
  artifacts:
    - services/worker/src/neuralcad_worker/tasks/geometry.py
    - services/worker/src/neuralcad_worker/geometry/measure.py
  key_links:
    - "String do `send_task` na API coincide com o `name=` do decorator da task no worker (ex.: ambos `process_geometry_job`)."
---

<objective>
**Worker Celery** com **pythonocc-core**: (1) construir sólido protótipo (caixa parametrizável a partir de `payload_json.target_dimensions_mm` ou default **10×20×30 mm**), (2) exportar STEP para bucket MinIO, (3) executar medição bbox (D-02) e montar `dimensional_audit` (D-03, D-04, D-05 espessura opcional `null`), (4) atualizar linha `jobs` no Postgres. Cumpre FOUND-02 (processamento) e FOUND-04 (artefacto + metadados).

Purpose: Fechar pipeline geométrico mínimo com **fidelidade dimensional auditável** conforme `01-CONTEXT.md`, sem integrar BrepGen real (placeholder extensível).

Output: Código worker + Dockerfile com OCC instalável em build.
</objective>

<threat_model>
| threat_id | category | component | disposition | mitigation_plan |
|-----------|-----------|-----------|-------------|-----------------|
| T-07 | Information disclosure | STEP em MinIO | Mitigate | Keys de objeto com prefixo `jobs/{id}/`; bucket privado; credenciais só env |
| T-08 | Unsafe deserialization | payload_json | Mitigate | Ler apenas chaves esperadas; ignorar campos desconhecidos; não `pickle` |
| T-09 | Denial of service | OCC pesado | Mitigate | Timeout de task Celery + `time_limit` configurável via env (documentar) |
</threat_model>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-geometric-pipeline-platform/01-CONTEXT.md
@.planning/REQUIREMENTS.md
@IDEA.md
@services/api/src/neuralcad_api/db/models.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Worker Python, Celery, Dockerfile com OCC</name>
  <files>services/worker/pyproject.toml, services/worker/Dockerfile, services/worker/src/neuralcad_worker/celery_app.py, docker-compose.yml</files>
  <read_first>services/worker/Dockerfile, services/api/src/neuralcad_api/main.py, docker-compose.yml</read_first>
  <action>Criar `services/worker/pyproject.toml` com `celery`, `redis`, `sqlalchemy`, `psycopg2-binary`, `boto3`, `pydantic>=2`, `pythonocc-core` (fixar versão compatível em comentário README worker). `Dockerfile`: base `python:3.10-slim`, instalar dependências de sistema necessárias para `pythonocc-core` (lista explicitamente pacotes `libocct-*-dev` **ou** seguir bloco de instalação oficial copiado na documentação interna `services/worker/README.md` — ficheiro novo permitido). `celery_app.py`: broker/backend a partir de `CELERY_BROKER_URL`. Registar `process_geometry_job` com `@celery_app.task(name="process_geometry_job", bind=True)` (ou sem bind se preferir) no módulo `neuralcad_worker.tasks.geometry`, coincidindo com `send_task("process_geometry_job", ...)` da API. Atualizar `docker-compose.yml` para `worker` com `build: ./services/worker`, env `DATABASE_URL` (sync `postgresql+psycopg2://...`), `MINIO_ENDPOINT`, `MINIO_ACCESS_KEY`, `MINIO_SECRET_KEY`, `MINIO_BUCKET=neuralcad-artifacts`.</action>
  <verify>grep -q celery services/worker/pyproject.toml; grep -q pythonocc services/worker/pyproject.toml</verify>
  <acceptance_criteria>
    - `services/worker/pyproject.toml` contém `pythonocc-core`
    - `services/worker/src/neuralcad_worker/celery_app.py` contém a string `Celery(`
    - `docker-compose.yml` contém a string `MINIO_BUCKET` ou variável equivalente documentada para o worker
  </acceptance_criteria>
  <done>Worker instanciável no Compose.</done>
</task>

<task type="auto">
  <name>Task 2: Stub sólido + medição + STEP</name>
  <files>services/worker/src/neuralcad_worker/geometry/stub_solid.py, services/worker/src/neuralcad_worker/geometry/measure.py</files>
  <read_first>.planning/phases/01-geometric-pipeline-platform/01-CONTEXT.md</read_first>
  <action>Em `stub_solid.py`: função `build_box_shape(dx_mm: float, dy_mm: float, dz_mm: float) -&gt; TopoDS_Shape` usando API OCC pública (`BRepPrimAPI_MakeBox`). Em `measure.py`: função `measure_bbox_mm(shape) -&gt; dict` usando `BRepBndLib.Add` e `bnd.Get()` para obter `xmin,ymin,zmin,xmax,ymax,zmax` e devolver `{"width": float, "height": float, "depth": float}` com **convenção fixa documentada** no docstring: e.g. width=xmax-xmin, height=ymax-ymin, depth=zmax-zmin. Export STEP com `STEPControl_Writer` para ficheiro temporário bytes.</action>
  <verify>grep -q BRepPrimAPI_MakeBox services/worker/src/neuralcad_worker/geometry/stub_solid.py</verify>
  <acceptance_criteria>
    - `measure.py` contém a string `BRepBndLib` ou `Bnd_Box`
    - `stub_solid.py` contém `BRepPrimAPI_MakeBox`
  </acceptance_criteria>
  <done>Geometria mínima reprodutível.</done>
</task>

<task type="auto">
  <name>Task 3: Task Celery + MinIO + audit + DB</name>
  <files>services/worker/src/neuralcad_worker/tasks/geometry.py, services/worker/src/neuralcad_worker/storage/minio_client.py, services/worker/src/neuralcad_worker/db/sync_session.py</files>
  <read_first>services/api/src/neuralcad_api/db/models.py, .planning/phases/01-geometric-pipeline-platform/01-CONTEXT.md</read_first>
  <action>Implementar `process_geometry_job(job_id: str)` que: (1) lê `Job` por id; status `running`; (2) parse `payload_json` para opcional `constraints.dimensionsMm` com chaves `width`,`height`,`depth` (nomes alinhados IDEA); se ausente usar 10,20,30; construir shape; (3) export STEP; upload `boto3` `put_object` key `jobs/{job_id}/model.step`; (4) calcular `measured` com `source: "pythonocc"` e timestamp ISO8601 UTC; (5) se target presente, calcular `delta` por eixo e `withinTolerance` segundo regra D-04: por eixo OK se `abs(m-t) &lt;= max(0.5, 0.01*t)` quando `t&gt;0`; (6) `thickness_mm: null`, `thickness_reason: "not_implemented"` no audit até implementação; (7) update `artifact_key`, `dimensional_audit`, `status='success'`. Em falha OCC: `status=failed`, mensagem truncada. Usar sessão SQLAlchemy sync reflectindo mesmos nomes de tabela/colunas que API.</action>
  <verify>grep -q dimensional_audit services/worker/src/neuralcad_worker/tasks/geometry.py</verify>
  <acceptance_criteria>
    - `geometry.py` contém a string literal `withinTolerance`
    - `geometry.py` contém a string literal `pythonocc`
    - `minio_client.py` ou `geometry.py` contém `put_object`
  </acceptance_criteria>
  <done>Pipeline ponta a ponta no worker.</done>
</task>

</tasks>

<verification>
- [ ] `docker compose up` com api+worker: `POST /api/v1/jobs` seguido de polling até `success` e `dimensional_audit.measured` presente
- [ ] Objeto `model.step` visível no bucket MinIO (mc ls ou consola)
</verification>

<success_criteria>

- FOUND-04 e parte operacional de FOUND-02 verificáveis
- Decisões D-01–D-08 do CONTEXT refletidas no código ou comentário TODO explícito apenas onde impossível (ex. BrepGen real)
  </success_criteria>

<output>
Após conclusão, criar `.planning/phases/01-geometric-pipeline-platform/01-03-SUMMARY.md`
</output>

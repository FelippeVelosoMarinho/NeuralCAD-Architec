---
phase: 01-geometric-pipeline-platform
plan: "02"
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - services/api/pyproject.toml
  - services/api/src/neuralcad_api/main.py
  - services/api/src/neuralcad_api/schemas/jobs.py
  - services/api/src/neuralcad_api/db/models.py
  - services/api/src/neuralcad_api/db/session.py
  - services/api/alembic.ini
  - services/api/alembic/env.py
  - services/api/alembic/versions/001_initial_jobs.py
  - services/api/Dockerfile
  - docker-compose.yml
autonomous: true
requirements:
  - FOUND-02
  - FOUND-03
user_setup: []
must_haves:
  truths:
    - "POST `/api/v1/jobs` com JSON `{}` ou corpo mínimo regista job `pending` e devolve `id` UUID."
    - "GET `/api/v1/jobs/{id}` devolve estado e campos preparados para `dimensional_audit` (podem ser null)."
  artifacts:
    - services/api/pyproject.toml
    - services/api/src/neuralcad_api/main.py
  key_links:
    - "API envia tarefa Celery `process_geometry_job` (ou nome equivalente definido no código) com o id do job."
---

<objective>
Serviço **FastAPI** com SQLAlchemy 2.x (async), fila **Celery** via Redis, e modelos de job/artefacto em Postgres alinhados a `01-CONTEXT.md`: coluna JSONB `dimensional_audit` nullable. Cumprir FOUND-02 (enfileirar) e FOUND-03 (cliente HTTP cria job e obtém id/estado). Atualizar Dockerfile API para usar `uvicorn`.

Purpose: Camada de controlo síncrona separada do worker OCC.

Output: API executável no Compose com migração inicial e endpoints mínimos.
</objective>

<threat_model>
| threat_id | category | component | disposition | mitigation_plan |
|-----------|-----------|-----------|-------------|-----------------|
| T-04 | Spoofing | API aberta | Mitigate | Sem auth na Fase 1; README “dev only”; preparar comentário `# TODO: auth phase N` em `main.py` |
| T-05 | Injection | SQL | Mitigate | SQLAlchemy ORM apenas; sem SQL raw |
| T-06 | DoS | Job flood | Mitigate | Opcional: limite de body size FastAPI (documentar default Starlette) |
</threat_model>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-geometric-pipeline-platform/01-CONTEXT.md
@.planning/REQUIREMENTS.md
@IDEA.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Projeto Python API + dependências</name>
  <files>services/api/pyproject.toml, services/api/Dockerfile, docker-compose.yml</files>
  <read_first>services/api/Dockerfile, docker-compose.yml, .planning/phases/01-geometric-pipeline-platform/01-CONTEXT.md</read_first>
  <action>Criar `services/api/pyproject.toml` com nome pacote `neuralcad-api`, Python `^3.10`, dependências: `fastapi`, `uvicorn[standard]`, `sqlalchemy[asyncio]`, `asyncpg`, `alembic`, `pydantic>=2`, `celery`, `redis`, `boto3` (MinIO S3). Definir pacote `src` layout: `services/api/src/neuralcad_api/`. Atualizar `services/api/Dockerfile`: copiar `pyproject.toml`, `pip install .`, `COPY src`, `CMD ["uvicorn", "neuralcad_api.main:app", "--host", "0.0.0.0", "--port", "8000"]`. Se necessário, ajustar `docker-compose.yml` para `api` com variáveis `DATABASE_URL`, `REDIS_URL`, `CELERY_BROKER_URL` iguais a `REDIS_URL`, `MINIO_ENDPOINT` (ex. `http://minio:9000`).</action>
  <verify>grep -q 'fastapi' services/api/pyproject.toml &amp;&amp; grep -q 'uvicorn' services/api/Dockerfile</verify>
  <acceptance_criteria>
    - `services/api/pyproject.toml` contém a string `pydantic`
    - `services/api/Dockerfile` contém `uvicorn` e `neuralcad_api.main:app`
  </acceptance_criteria>
  <done>pyproject + Dockerfile API funcionais para build.</done>
</task>

<task type="auto">
  <name>Task 2: Modelos DB, migração, endpoints jobs</name>
  <files>services/api/src/neuralcad_api/db/models.py, services/api/src/neuralcad_api/db/session.py, services/api/src/neuralcad_api/main.py, services/api/src/neuralcad_api/schemas/jobs.py, services/api/alembic.ini, services/api/alembic/env.py, services/api/alembic/versions/001_initial_jobs.py</files>
  <read_first>.planning/phases/01-geometric-pipeline-platform/01-CONTEXT.md, IDEA.md</read_first>
  <action>Implementar modelo `Job` com colunas: `id` UUID PK, `status` string (valores: `pending`, `running`, `success`, `failed`), `created_at`, `updated_at`, `payload_json` JSONB opcional (aceitar `intent.constraints.dimensionsMm` e `thicknessMm` espelhados do IDEA § IntentSchema sem validar schema completo), `error_message` text nullable, `artifact_key` string nullable (path MinIO), `dimensional_audit` JSONB nullable. Criar migração Alembic `001_initial_jobs` que cria tabela `jobs` com tipos PostgreSQL corretos. `main.py`: `lifespan` ou startup que corre migrations via subprocess **ou** documenta `alembic upgrade head` no entrypoint Docker — preferir script `entrypoint.sh` que executa `alembic upgrade head` antes de uvicorn. Rotas: `POST /api/v1/jobs` corpo JSON opcional; cria registo `pending`; chama `celery_app.send_task("process_geometry_job", args=[str(job_id)])` onde o worker regista a task com `@celery_app.task(name="process_geometry_job")` (mesmo literal obrigatório no plano 01-03). `GET /api/v1/jobs/{job_id}` devolve serialização Pydantic incluindo `dimensional_audit`.</action>
  <verify>python -c "import ast" nos ficheiros principais; ou pytest placeholder</verify>
  <acceptance_criteria>
    - `services/api/src/neuralcad_api/db/models.py` contém a string literal `dimensional_audit`
    - `services/api/src/neuralcad_api/main.py` contém a string literal `/api/v1/jobs`
    - `services/api/alembic/versions/001_initial_jobs.py` existe e contém `op.create_table` e `jobs`
  </acceptance_criteria>
  <done>Migração + rotas mínimas prontas.</done>
</task>

</tasks>

<verification>
- [ ] Build `docker compose build api` com sucesso
- [ ] Teste manual ou pytest: criação de job retorna UUID
</verification>

<success_criteria>

- FOUND-02 e FOUND-03 cobertos ao nível API+fila
- JSONB `dimensional_audit` presente no modelo
  </success_criteria>

<output>
Após conclusão, criar `.planning/phases/01-geometric-pipeline-platform/01-02-SUMMARY.md`
</output>

---
phase: 04-realtime-orchestration
plan: "02"
type: execute
wave: 2
depends_on: ["01"]
autonomous: true
requirements:
  - RT-01
  - RT-02
phase_context: ".planning/phases/04-realtime-orchestration/04-CONTEXT.md"
must_haves:
  truths:
    - "Todos os enums de pipeline PROMPTING…MEASURING_ABNT emitidos no stub OCC na ordem ancoradas em geometry.py conforme CONTEXT."
    - "eventos lifecycle (running/success/failed/cancelled) coerentes com commits DB."
---

<objective>

**Realtime publisher Wave 2:** centralizar **`publish_job_channel_message`** Redis JSON; **`process_geometry_job`** emite **pipeline stages obrigatórios** + lifecycle; respeitar **cancelamento** entre etapas quando flag/celery revoke aplicável.

</objective>

<tasks>

### 04-02-01 — Pacote realtime worker

- `neuralcad_worker/realtime.py`: `publish(job_id, envelope_dict)` — JSON serializável, `schemaVersion:"1"`, `ts` ISO.

- Envelope types mínimos: **`job.lifecycle`**, **`job.progress`**, **`job.cancelled`** (eco se API também publica — idempotência ok).

- Campo progress: **`detail.pipelineStage`** ∈ {`PROMPTING`,`DENOISING_FACES`,`DENOISING_EDGES`,`SEWING`,`MEASURING_ABNT`}.

### 04-02-02 — Cancelamento cooperativo

- Entre estágios: recarregar job; se `status == cancelled` → publicar nada extra além de lifecycle já definido e sair.

### 04-02-03 — Hooks geometry.py

- Após commit `running` → lifecycle `running`.

- Ancoragens CONTEXT D‑04‑07.

- Failure path lifecycle `failed` + erro chunk.

- Success path lifecycle `success` antes de último publish.

### 04-02-04 — Harmonizar com DELETE

- Garantir publicação **`job.cancelled`** duplicada API/worker não corrompe clientes (**order** deterministic).

### 04-02-05 — Smoke / fakeredis pytest

---

</tasks>

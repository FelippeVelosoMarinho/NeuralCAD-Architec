---
phase: 04-realtime-orchestration
plan: "02"
type: execute
wave: 2
depends_on: ["01"]
autonomous: true
requirements:
  - RT-01
  - RT-02
must_haves:
  truths:
    - "Transições de estado do job publicam evento JSON no canal Redis do job."
    - "Payload inclui jobId, status e timestamp."
---

<objective>
**Worker Celery** publica eventos no mesmo canal Redis que o gateway WS consome: `job.started`, `job.completed`, `job.failed` (opcional `job.progress`).

Output: `realtime.py` reutilizável + hooks em `process_geometry_job` após commits coerentes.

</objective>

<tasks>

### 04-02-01 — publish_job_event

- Módulo `neuralcad_worker/realtime.py`, JSON estável `schemaVersion`, `type`, `jobId`, `status`, `ts`, `detail`.

### 04-02-02 — Hooks

- Em `geometry.py`: publicar nos pontos início/sucesso/falha depois das actualizações de estado persistidas.

### 04-02-03 — Smoke

- fakeredis ou mock; comandos documentados.

</tasks>

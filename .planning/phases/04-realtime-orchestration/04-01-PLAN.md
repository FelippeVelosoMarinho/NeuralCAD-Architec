---
phase: 04-realtime-orchestration
plan: "01"
type: execute
wave: 1
depends_on: []
autonomous: true
requirements:
  - RT-01
user_setup:
  - Docker Compose com Redis acessível à API (já existente).
must_haves:
  truths:
    - "Existe endpoint WebSocket que aceita assinatura a um jobId."
    - "Conexão recebe mensagens JSON após subscrição (evento synthetic subscribed até 04-02 publicar)."
  key_links:
    - ".planning/REQUIREMENTS.md RT-01"
---

<objective>
Gateway **WebSocket** na API para subscrição a eventos de um **job**, com `client_session` opcional (UUID) em query. Base para consumo Redis Pub/Sub no plano **04-02**.

Out of scope: publicação pelo worker neste PR; frontend (**04-03**).

Output: rota WS documentada + subscriber Redis async ligado ao canal `neuralcad:job:{job_id}`.

</objective>

<tasks>

### 04-01-01 — Infra código

- Criar pacote/módulo `neuralcad_api/ws/` com handler WS (aceitar conn, validar `job_id` existe em Postgres, opcionalmente `client_session`).

### 04-01-02 — Redis async subscriber

- Usar `redis.asyncio` com URL a partir de `CELERY_BROKER_URL` ou env dedicada.
- Após `accept`, enviar `{"type":"subscribed","jobId":"..."}` e escutar `neuralcad:job:{id}` encaminhando mensagens ao cliente.

### 04-01-03 — Registo em `main.py`

- Registrar rota WS; não quebrar CORS para HTTP.

### 04-01-04 — Verificação

- Teste asyncio mínimo ou instruções `wscat` no SUMMARY.

</tasks>

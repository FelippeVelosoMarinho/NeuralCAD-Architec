---
phase: 04-realtime-orchestration
plan: "03"
type: execute
wave: 3
depends_on: ["01", "02"]
autonomous: true
requirements:
  - RT-02
  - RT-03
phase_context: ".planning/phases/04-realtime-orchestration/04-CONTEXT.md"
must_haves:
  truths:
    - "Polling removido; GET job só reconnect / erro WS segundo CONTEXT."
    - "TanStack Query + WS com client_session uuid persistido sessionStorage por aba."
    - "Painel/engineering stage label derivado último job.progress.pipelineStage quando existir."
---

<objective>

**SPA Wave 3:** remover `setInterval` GET; cliente WS com **`client_session` obrigatório** derivado novo UUID por navegação; **re-sync GET único** em falhas WS antes de novo socket; opcional etiqueta texto da etapa PROMPTING…MEASURING_ABNT.

</objective>

<tasks>

### 04-03-01 TanStack bootstrap

### 04-03-02 wsJobChannel com query `client_session`

### 04-03-03 useJobFlow refactor

- Remover intervalo 2s.

- Após `POST` job: `queryClient.fetchQuery` snapshot.

- Abrir WS; em `onerror` / após N falhas consecutivas: **um** `refetch` manual.

- Em `job.cancelled` / terminal: fechar WS, invalidar query.

### 04-03-04 UI mínima engenharia

- Mostrar string `pipelineStage` bruta no painel status (até design system).

### 04-03-05 Vitest mocks

### 04-03-06 SUMMARY manual cross-browser duas abas mesmo jobId

---

</tasks>

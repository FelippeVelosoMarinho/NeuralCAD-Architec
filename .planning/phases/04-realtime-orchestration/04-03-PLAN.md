---
phase: 04-realtime-orchestration
plan: "03"
type: execute
wave: 3
depends_on: ["01", "02"]
autonomous: true
requirements:
  - RT-02
  - RT-03
must_haves:
  truths:
    - "Fluxo feliz usa WebSocket como fonte principal de estado do job na UI."
    - "TanStack Query sincroniza cache com servidor via invalidação ou patch a partir do WS."
---

<objective>
Integrar **TanStack Query** + **WebSocket** em `useJobFlow`; polling só como reconciliação / fallback curto.

Out of scope: barra de progresso rica (texto do estado chega para MVP).

</objective>

<tasks>

### 04-03-01 — Provider

- `@tanstack/react-query` + `QueryClientProvider`.

### 04-03-02 — wsJobChannel

- URL `ws` derivada de `VITE_API_BASE_URL`; reconnect com backoff.

### 04-03-03 — useJobFlow

- Query do job após criar; subscrição WS; transição terminal invalida/atualiza e mantém fluxo mesh.

### 04-03-04 — Vitest

- Mock `WebSocket`.

</tasks>

POSTGRES_USER=neuralcad
POSTGRES_PASSWORD=change_me
POSTGRES_DB=neuralcad
MINIO_ROOT_USER=minio
MINIO_ROOT_PASSWORD=change_me_minio
REDIS_URL=redis://redis:6379/0
DATABASE_URL=postgresql+asyncpg://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}
DATABASE_URL_SYNC=postgresql://neuralcad:change_me@postgres:5432/neuralcad
CELERY_BROKER_URL=redis://redis:6379/0
CELERY_RESULT_BACKEND=redis://redis:6379/0
MINIO_ENDPOINT=http://minio:9000
MINIO_ACCESS_KEY=minio
MINIO_SECRET_KEY=change_me_minio
MINIO_BUCKET=neuralcad-artifacts
HOST_API_PORT=8010
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-sonnet-4-20250514
# PROMPT_LLM_BACKEND=anthropic | cursor ; com cursor usar: docker compose --profile cursor up
PROMPT_LLM_BACKEND=cursor #anthropic
# Dentro da rede Compose (API em container — omissão do docker-compose):
CURSOR_PROMPT_BRIDGE_URL=http://prompt-bridge:3040
# Se correres a API no host (`uvicorn` local), não uses o hostname Docker — antes falha DNS (-3).
# Neste caso usa a porta publicada no host (HOST_PROMPT_BRIDGE_PORT):
# CURSOR_PROMPT_BRIDGE_URL=http://127.0.0.1:3040
CURSOR_API_KEY=
CURSOR_MODEL_ID=composer-2
CURSOR_AGENT_CWD=/workspace
CURSOR_BRIDGE_TIMEOUT_SECONDS=600
HOST_PROMPT_BRIDGE_PORT=3040
